
:LastChangedDate: $LastChangedDate$
:LastChangedRevision: $LastChangedRevision$
:LastChangedBy: $LastChangedBy$

Twisted Web In 60 Seconds
=========================

This set of examples contains short, complete applications of :api:`twisted.web <twisted.web>` .
For subjects not covered here, see the :doc:`Twisted Web tutorial <../using-twistedweb>` and the API documentation.

.. toctree::
  `Serving static content from a directory <static-content>`
  `Generating a page dynamically <dynamic-content>`
  `Static URL dispatch <static-dispatch>`
  `Dynamic URL dispatch <dynamic-dispatch>`
  `Error handling <error-handling>`
  `Custom response codes <custom-codes>`
  `Handling POSTs <handling-posts>`
  `Other request bodies <other-request-bodies>`
  `rpy scripts (or, how to save yourself some typing) <rpy-scripts>`
  `Asynchronous responses <asynchronous>`
  `Asynchronous responses (via Deferred) <asynchronous-deferred>`
  `Interrupted responses <interrupted>`
  `Logging errors <logging-errors>`
  `Access logging <access-logging>`
  `WSGIs <wsgi>`
  `HTTP authentication <http-auth>`
  `Session basics <session-basics>`
  `Storing objects in the session <session-store>`
  `Session endings <session-endings>`

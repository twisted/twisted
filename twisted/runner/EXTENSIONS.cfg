extensions = []
if builder.check_header("rpc/rpc.h"):
    extensions.append(
        Extension("twisted.runner.portmap",
                  ["twisted.runner/portmap.c"])) # Note: twisted.runner/portmap.c
else:
    self.announce("Sun-RPC portmap support is unavailable on this system "
                  "(but that's OK, you probably don't need it anyway).")

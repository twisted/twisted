#
# These are the minimum versions of dependencies that we support.
# They include extra dependencies so that we test functionality for TLS, HTTP2,
# or SSH.
#
# NOTE: This file needs to be manually kept in sync with the versions defined
#       in pyproject.toml - for an eventual automated solution, see
#       https://github.com/jazzband/pip-tools/issues/2464

#
# Hard dependencies.
#
zope.interface == 8.0.0
constantly == 15.1
incremental == 24.7.1
Automat == 24.8.1
hyperlink == 18.0.0
attrs == 22.2.0
typing_extensions == 4.13.2

#
# For [tls]
#
pyopenssl == 25.3.0
six==1.16.0
service_identity == 21.1.0
idna == 2.5

#
# For [conch]
#
cryptography == 46
appdirs == 1.4.0
bcrypt == 3.2.1

#
# For [http2]
#
h2 == 3.2
priority == 1.1.0

#
# For [websocket]
#
wsproto == 1.2.0


#
# For [test]
#
cython-test-exception-raiser == 26.4.1
PyHamcrest == 2.0
hypothesis == 6.56


# we just use 'httpx' as a 3rd-party HTTP client in one http2 test, so we need
# httpx[http2] but constraints files may not contain extras.  All these
# dependencies are transitive dependencies of this *one* dep, that are not
# pinned elsewhere in this file, which is not used by Twisted at all, so these
# are all just recent as of 2026-08-19, manually generated by `pip install
# 'httpx[http2]==0.27.0'; pip freeze` in a temporary python3.10 venv .

httpx == 0.27.0
anyio==1.3.1
certifi==2026.7.22
exceptiongroup==1.3.1
h11==0.16.0
hpack==3.0.0
httpcore==1.0.9
hyperframe==5.2.0
sniffio==1.3.1

coverage==7.15.4

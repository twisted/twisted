python:
    x = 1
python:
    print x+1
    class Foo:
        def test(self):
            print 'testtesttesttest'

# node is always the matched node
# document is always the root document
# python code always begins with one level of indentation

node   /html/head: # select the head node by absolute path
    node.appendChild(document.createTextNode('hi')) # mutate it
    print document.toxml()

    1+1;print 'hi'
    print Foo().test()

node   //div :  # clean up whitespace around xpath expressions
    print 'div' # there are mult. divs so this should run mult. times

node /html[head]: # var. xpath idioms, select the html node that contains a head
    if node:  # test different levels of indentation
        print node.name

# test whitespace at the end of the line.
node //foo:      
    #      ^^^^^^ do not erase this whitespace
    pass

node /html[foo]: # not a real node..
    1/0     # this should never get executed

node //*[name() != 'div']: # any non-div node -- test internal spaces
    print '!div', node.name

# test comment parsing
node //*[string-length(name()) > string-length('   #')]:
    print node.name
